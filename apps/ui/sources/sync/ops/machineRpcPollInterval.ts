/**
 * How often the app asks a machine again while it awaits an answer the machine only reports on
 * request (spawn-nonce resolution, a `tool.systemTasks` task result).
 */
export const MACHINE_RPC_POLL_INTERVAL_MS = 1_000;
